# Postman Tutorial API

A simple FastAPI + PostgreSQL CRUD application designed for learning API testing using Postman.

## Features
- GET /items
- GET /items/{id}
- POST /items
- PUT /items/{id}
- DELETE /items/{id}
- Fully dockerized (API + DB)

## Requirements
- Docker Desktop installed

## How to Run

```bash
docker-compose up --build